// TabletLoader.jsx
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const TabletLoader = () => {
  return (
    <View style={styles.tablet-loader}>
      <Text>Tablet Loader</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  tableLoader: {
    /* Add tablet-specific loader styles using React Native StyleSheet */
  },
});

export default TabletLoader;
