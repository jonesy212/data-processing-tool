// ScheduledData.ts
export interface ScheduledData {
    id: string | number;
    title: string;
    description: string;
    scheduledDate: Date;
    createdBy: string;
    // Add other properties as needed
  }
  