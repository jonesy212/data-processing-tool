// StrategyOptions.ts

import TradingStrategy, { TradingStrategyOptions } from "./TradingStrategy";


// Now you can use strategyOptions in your code
const tradingStrategy = new TradingStrategy(strategyOptions, initialCapital);
