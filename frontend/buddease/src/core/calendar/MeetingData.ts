// MeetingData.ts
// Define the interface for MeetingData
export interface MeetingData {
    title: string;
    date: Date;
    duration: number;
    description: string;
    participants: string[];
  }

  
//  MeetingData, on the other hand, specifically represents data related to meetings. It includes properties such as title, date, duration, description, and participants. Meetings are a specific type of scheduled event where people gather to discuss specific topics or agendas.

