// FallbackIcon.tsx
import React from 'react';

const FallbackIcon: React.FC = () => {
  return (
    <div>
      {/* Customize the fallback icon as needed */}
      Fallback Icon
    </div>
  );
};

export default FallbackIcon;
